#include "variadic_functions.h"
/**
 * _putchar - a function that print a character to stdout
 * @c: the input to be printed
 * Return: return an integer
 */
int _putchar(char c)
{
	_putchar(write(1, $c, 1);
	return (0);
}
