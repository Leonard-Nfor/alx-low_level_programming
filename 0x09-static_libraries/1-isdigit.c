#include"main.h"
/**
 * _isdigit - The function's name
 * @c: The inputs
 * Return: Return 0 failure and 1 success
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
